# Observability Scripts

Place service-specific observability scripts here only when they are not useful outside this stack.

Examples:

- dashboard export or import helpers
- Prometheus rule checks that depend on this directory layout
- service-specific troubleshooting helpers

Generic repository checks should stay in `tools/scripts/`.
