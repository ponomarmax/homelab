# Node Exporter Config

Node Exporter is expected to stay stateless.

Future checkpoints should enable only the collectors needed for this single-node host.
Avoid broad host changes unless they are documented and reproducible.
