# Prometheus Config

Future Prometheus configuration belongs here.

Expected files later:

- `prometheus.yml` - scrape configuration.
- `rules/` - alerting or recording rules when they become useful.

Retention should be limited from the first real Prometheus checkpoint.
The current default target is 15 days unless the constraints document changes.
