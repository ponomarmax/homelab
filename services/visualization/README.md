# Visualization

Placeholder for lightweight validation and visualization views.

Early scope:

- list sessions
- show recent chunks
- show simple stream-level charts after parsing exists
- help compare collector debug state with backend storage

This area is intentionally separate from ingestion so accepting raw data does not depend on charting or analytics.
